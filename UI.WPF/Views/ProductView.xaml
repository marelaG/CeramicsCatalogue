<UserControl x:Class="GancewskaKerebinska.CeramicsCatalogue.UI.WPF.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GancewskaKerebinska.CeramicsCatalogue.UI.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Products}" 
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Ceramic Type" Binding="{Binding CeramicType}" />
                <DataGridTextColumn Header="Firing Type" Binding="{Binding FiringType}" />
                <DataGridTextColumn Header="Producer" Binding="{Binding Producer.Name}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>