<Window x:Class="GancewskaKerebinska.CeramicsCatalogue.UI.WPF.Views.ProductEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Editor" Height="350" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Name:" Grid.Row="0"/>
        <TextBox x:Name="NameTextBox" Grid.Row="0" Margin="100,0,0,5"/>

        <Label Content="Ceramic Type:" Grid.Row="1"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="1" Margin="100,0,0,5"/>

        <Label Content="Firing Type:" Grid.Row="2"/>
        <ComboBox x:Name="FiringComboBox" Grid.Row="2" Margin="100,0,0,5"/>

        <Label Content="Producer:" Grid.Row="3"/>
        <ComboBox x:Name="ProducerComboBox" Grid.Row="3" Margin="100,0,0,5" 
                  DisplayMemberPath="Name" SelectedValuePath="Id"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Click="Save_Click" Width="80" Margin="5"/>
            <Button Content="Cancel" IsCancel="True" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>