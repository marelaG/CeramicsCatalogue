<Window x:Class="GancewskaKerebinska.CeramicsCatalogue.UI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GancewskaKerebinska.CeramicsCatalogue.UI.WPF"
        xmlns:viewModels="clr-namespace:GancewskaKerebinska.CeramicsCatalogue.UI.WPF.ViewModels"
        xmlns:views="clr-namespace:GancewskaKerebinska.CeramicsCatalogue.UI.WPF.Views"
        mc:Ignorable="d"
        Title="Ceramics Catalogue" Height="768" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        FontFamily="Segoe UI">

    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo / Title -->
                <StackPanel Grid.Row="0" Margin="0,0,0,40">
                    <TextBlock Text="Ceramics" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                    <TextBlock Text="Catalogue" FontSize="26" FontWeight="Light" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <!-- Navigation Menu -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="MENU" FontSize="12" FontWeight="Bold" Foreground="#9CA3AF" Margin="4,0,0,12"/>
                    <Button Content="Products" Command="{Binding ChangeViewCommand}" CommandParameter="Products" Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Producers" Command="{Binding ChangeViewCommand}" CommandParameter="Producers" Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>
                
                <!-- Footer -->
                <TextBlock Grid.Row="2" Text="v1.0.0" Foreground="{StaticResource TextSecondary}" FontSize="12" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar: Title & Search -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Dashboard" FontSize="30" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Width="240" 
                             Style="{StaticResource SearchTextBoxStyle}"
                             Margin="0,0,8,0"/>
                    <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryActionButtonStyle}"/>
                </StackPanel>
            </Grid>

            <!-- Action Toolbar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,24">
                <Button Content="+ Add New" Command="{Binding AddCommand}" Style="{StaticResource PrimaryActionButtonStyle}"/>
                <Button Content="Modify Selected" Command="{Binding ModifyCommand}" Style="{StaticResource ActionButtonStyle}"/>
                <Button Content="Delete Selected" Command="{Binding DeleteCommand}" Style="{StaticResource DangerActionButtonStyle}"/>
            </StackPanel>

            <!-- View Content -->
            <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.05" BlurRadius="10"/>
                </Border.Effect>
                <ContentControl Content="{Binding CurrentView}" Margin="20"/>
            </Border>
        </Grid>
    </Grid>
</Window>